version: "3.8"

services:
  frontend-apollo:
    container_name: frontend-apollo
    restart: on-failure
    image: api-apollo:latest
    ports:
      - "80:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1g
